//Gauss's formula to find sum of series starting at 1: 
    // S = N(N + 1)/2



function SimpleAdding(num) { 

    // code goes here  
    num = (num * (num + 1))/2;
    return num; 
           
  }
     